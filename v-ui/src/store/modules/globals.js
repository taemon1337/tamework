// import { GlobalTypes } from '@/store/mutation-types'

const state = {
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations must be synchronous
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
